# Research Scripts

Debugging and memory discovery tools used during development of halo2-stats.
Not part of the production tool.

These scripts must be run from the project root or they use a `sys.path` shim
to find `xbdm_client.py` in the parent directory.

See `RESEARCH.md` in the project root for detailed findings from these tools.
